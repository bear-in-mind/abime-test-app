<%= simple_form_for @team do |f| %>
  <%= f.input :name, label: "Team name" %>
    <h1>Players</h1>
      <!-- INSERT PARENT AND CHILD MODEL NAMES IN DATA ATTRIBUTES -->
    <%= render NestedFormComponent.new(form: f, association: Player.new) %>
  <%= f.submit class: "mt-3 btn btn-blue" %>
<% end %>
